# Реализация таймера игры 🎾⏱️

## Обзор функциональности

Реализована полная система таймеров для матча паделя:
- **Таймер матча** - считает общее время всей игры
- **Таймер раунда/сета** - считает время каждого отдельного раунда
- **История раундов** - сохраняет статистику каждого раунда

## Основные компоненты

### 1️⃣ Переменные состояния

```dart
// Таймер матча
Timer? _timer;
Duration _duration = Duration.zero;

// Таймер раунда/сета
Timer? _roundTimer;
Duration _roundDuration = Duration.zero;
bool _roundTimerActive = false;

// История раундов/сетов с их временем
List<Map<String, dynamic>> rounds = [];
```

### 2️⃣ Методы управления таймерами

#### `_startTimer()` - Запуск таймера матча
- Запускается в `initState()`
- Считает время от начала матча
- Обновляется каждую секунду

#### `_startRoundTimer()` - Начать таймер раунда
- Запускается при нажатии кнопки "Начать раунд"
- Устанавливает флаг `_roundTimerActive = true`
- Тикает каждую секунду

#### `_stopRoundTimer()` - Остановить таймер раунда
- Останавливает текущий раунд
- Устанавливает флаг `_roundTimerActive = false`
- Используется перед завершением раунда

#### `_finishRound()` - Завершить раунд и записать статистику
- Остановит таймер раунда
- Сохраняет данные раунда в список `rounds`:
  - Номер раунда
  - Длительность раунда
  - Счет обоих игроков в этом раунде
  - Временную метку

#### `_startNewRound()` - Начать новый раунд
- Останавливает предыдущий раунд
- Сбрасывает счетчик времени раунда
- Запускает новый таймер

### 3️⃣ UI компоненты

#### Табло времени матча
```
ВРЕМЯ МАТЧА
00:12:34  (ярко-зеленое, моноширинный шрифт)
```

#### Табло времени раунда
```
ВРЕМЯ РАУНДА
00:02:15

[Начать раунд] [Завершить раунд]
```
- Показывает синий фон, если раунд не активен
- Показывает красный фон, если раунд активен
- Кнопки активны/неактивны в зависимости от состояния

#### История раундов
```
📊 История раундов
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│ Раунд 1     │ │ Раунд 2     │ │ Раунд 3     │
│ 00:02:15    │ │ 00:02:30    │ │ 00:01:45    │
│ 6-4         │ │ 7-5         │ │ 5-6         │
└─────────────┘ └─────────────┘ └─────────────┘
```
- Горизонтальный список раундов
- Показывает номер, время и счет каждого раунда

### 4️⃣ Сохранение статистики

При завершении матча в БД сохраняется:

```json
{
  "status": "FINISHED",
  "score": "12-11",
  "match_stats": {
    "total_rounds": 3,
    "total_match_time": "00:06:40",
    "rounds_data": [
      {
        "number": 1,
        "duration": "00:02:15",
        "score": "6-4"
      },
      {
        "number": 2,
        "duration": "00:02:30",
        "score": "7-5"
      },
      {
        "number": 3,
        "duration": "00:01:45",
        "score": "5-6"
      }
    ]
  }
}
```

## Использование

### Начать матч
1. Экран открывается → таймер матча автоматически запускается
2. Все таймеры идут в фоне

### Управление раундами
1. **Начать раунд** → нажимаете кнопку "Начать раунд"
2. **Увеличиваете счет** → нажимаете + для каждого игрока
3. **Завершить раунд** → нажимаете кнопку "Завершить раунд"
4. **История обновляется** → новый раунд появляется в списке

### Завершить матч
1. Если раунд еще активен, он автоматически завершится
2. Вся статистика сохранится в БД
3. Экран закроется

## Форматирование времени

Все таймеры используют формат **HH:MM:SS**:
- 00:00:05 = 5 секунд
- 00:01:30 = 1 минута 30 секунд
- 01:23:45 = 1 час 23 минуты 45 секунд

Реализовано методом `_formatDuration()`:
```dart
String _formatDuration(Duration duration) {
  String twoDigits(int n) => n.toString().padLeft(2, "0");
  String twoDigitMinutes = twoDigits(duration.inMinutes.remainder(60));
  String twoDigitSeconds = twoDigits(duration.inSeconds.remainder(60));
  return "${twoDigits(duration.inHours)}:$twoDigitMinutes:$twoDigitSeconds";
}
```

## Возможные улучшения

1. **Пауза/Возобновление** - добавить кнопку паузы для раундов
2. **Звуковые сигналы** - звук при начале/конце раунда
3. **Аналитика** - график времени раундов, средняя длительность
4. **Восстановление** - сохранить состояние при случайном закрытии
5. **Синхронизация** - показать таймеры обоих команд одновременно

## Файл для отладки

Все операции логируются в консоль браузера (F12 → Console):
- 🎾 Таймер раунда начат!
- ⏹️ Таймер раунда остановлен! Длительность: 00:02:15
- ✅ Раунд 1 завершен за 00:02:15
- 🔄 Reloading data after skills update...
